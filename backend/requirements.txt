fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.13.4  # floor set by google-genai 2.16.0 (>=2.12.5); 2.10.3 was
                  # unsatisfiable alongside it and pip refused the whole file
pydantic-settings==2.6.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.19
python-docx==1.1.2
# The CSR module's source extraction: protocols and TLF bundles arrive as PDF,
# and SAS statistical output arrives as RTF. Both are optional at import time --
# app/csr/extraction.py degrades with a named-package error rather than a crash --
# but a deployment that ingests real study documents needs them.
pymupdf==1.28.2
striprtf==0.0.33
pypdf==5.1.0
openpyxl==3.1.5
scikit-learn==1.5.2
numpy==2.1.3
anthropic==0.120.2  # >=0.100 required for structured outputs (output_config.format)
google-genai==2.16.0  # Gemini provider; response_json_schema takes plain JSON Schema
openai==3.3.1  # OpenAI provider; Responses API + strict json_schema structured outputs
babel==2.18.0  # locale-aware number/date formatting
beautifulsoup4==4.12.3
lxml==5.3.0
python-dotenv==1.0.1
psycopg[binary]==3.2.3
pgvector==0.4.1  # the `vector` column type for §10 semantic memory; import is guarded, see app/retrieval/embeddings.py
redis==5.2.1
alembic==1.14.0

# --- dev / test ---
pytest==9.1.1
httpx==0.28.1  # required by fastapi.testclient
pytest-cov==7.1.0  # coverage floors enforced by scripts/check.sh
PyYAML==6.0.3
